CREATE TABLE personer (
						userID int,
						userName varchar(40), 
                        ini varchar(6),
                        roles varchar(10),
                        cpr varchar(11),
                        passwd varchar(64)
                        );